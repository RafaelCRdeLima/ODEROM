[package]
name = "oderom-notebook"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "ODEROM notebook (Etapa 3, DESIGN-NOTEBOOK.md) -- block notebook logic over oderom-session, no window."

[dependencies]
oderom-cli = { path = "../oderom-cli" }
oderom-session = { path = "../oderom-session" }

[lints.rust]
warnings = "deny"
unsafe_code = "forbid"
